#!/usr/bin/env node

const fs = require('fs');
const path = require('path');

let value = fs.readFileSync(path.join(__dirname, '/index.js'), 'utf-8');

fs.writeFileSync(path.join(process.cwd(), '/' + process.argv[2] + '.js'), value,'utf-8');
